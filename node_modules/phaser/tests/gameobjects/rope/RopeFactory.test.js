var RopeFactory = require('../../../src/gameobjects/rope/RopeFactory');

describe('RopeFactory', function ()
{
    it('should be importable', function ()
    {
        expect(RopeFactory).toBeDefined();
    });
});
