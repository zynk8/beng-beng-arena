var Ellipse = require('../../../../src/gameobjects/shape/ellipse/Ellipse');

describe('Ellipse', function ()
{
    it('should be importable', function ()
    {
        expect(Ellipse).toBeDefined();
    });
});
