var StarFactory = require('../../../../src/gameobjects/shape/star/StarFactory');

describe('StarFactory', function ()
{
    it('should be importable', function ()
    {
        expect(StarFactory).toBeDefined();
    });
});
