var LayerCreator = require('../../../src/gameobjects/layer/LayerCreator');

describe('LayerCreator', function ()
{
    it('should be importable', function ()
    {
        expect(LayerCreator).toBeDefined();
    });
});
