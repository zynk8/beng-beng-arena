var GradientFactory = require('../../../src/gameobjects/gradient/GradientFactory');

describe('GradientFactory', function ()
{
    it('should be importable', function ()
    {
        expect(GradientFactory).toBeDefined();
    });
});
