var MeasureText = require('../../../src/gameobjects/text/MeasureText');

describe('Phaser.GameObjects.MeasureText', function ()
{
    it('should be importable', function ()
    {
        expect(MeasureText).toBeDefined();
        expect(typeof MeasureText).toBe('function');
    });
});
